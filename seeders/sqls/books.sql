CREATE TABLE `livros` (
  `id` int(11) NOT NULL,
  `titulo` varchar(60),
  `categoria` varchar(60),
  `autor` varchar(60),
  `editora` varchar(60),
  `tipo` varchar(60),
  `estado` tinyint(1)
  `iduser` int
);